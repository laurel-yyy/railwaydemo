spring.application.name=railway


# mybatis-plus.mapper-locations=classpath*:mapper/**/*.xml  # 配置 MyBatis 映射文件的位置（如果需要）
# mybatis-plus.type-aliases-package=com.example.demo.entity  # 设置实体类所在的包路径
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl  # 输出 SQL 日志（可选）


# MySQL 数据源配置
spring.datasource.url=jdbc:mysql://localhost:3306/railway?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis-Plus 配置
# mybatis-plus.mapper-locations=classpath:/mappers/*.xml 通过@Mapper 注解会告诉 MyBatis 扫描该接口
mybatis-plus.type-aliases-package=com.ourgroup.railway.services.dao

# spring:
#   datasource:
#     url: jdbc:mysql://mysql_railwaydemo:3390/railwaydemo
#     username: root
#     password: root
#     driver-class-name: com.mysql.cj.jdbc.Driver
#     hikari:
#       pool-name: HikariCP
#   jpa:
#     hibernate:
#       ddl-auto: update  # 自动创建数据库表（开发环境可以使用 'update'，生产环境建议使用 'validate' 或 'none'）
#     show-sql: true  # 打印 SQL 查询
#     database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

